import React from "react";
// initializing a new class component called App
class App extends React.Component {
  // Define the constructor method of the component
  constructor(props) {
    // Call the constructor method of the React.Component class with the props parameters
    super(props);
    // Initialize the state of the component with an empty object called person and using the this keyword to define the state
    this.state = {
      person: {}
    };
  }
  
  // Define the render method of the component
  render() {
    return ;
  }
}

export default App;
